# Camada LLM: client OpenAI-compatible (llm_client), runtime de agente
# LangGraph (agent_graph; agent_runtime é a facade que preserva o contrato
# AgentResult) e as tools por domínio (agent_tools/).
