# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.parquet

# Environment
.env
.env.*
!envs/.env.example
!envs/.env.local.example
!envs/.env.test.example
!envs/.env.staging.example
!envs/.env.staging-local.example
!envs/.env.production.example

# Cloudflare Tunnel — credentials sensíveis
cloudflared/

# Data (raw sources — não versionar)
data/bronze/
.data-staging/
# silver é derivado (não versionar), EXCETO as fontes curadas à mão do v3
# (§3 da spec docs/specs/v3-unified.md) — load-bearing, precisam de versão.
# Nota: "data/silver/*" (não "data/silver/") porque git não re-inclui arquivo
# dentro de diretório ignorado.
data/silver/*
!data/silver/programas.json
!data/silver/investidores.json
data/vocab_lint/
profiles/
artigos/
benchmark/
audit_finep_structure/

# Knowledge Graph — dados gerados
data/knowledge_graph/wiki/
data/knowledge_graph/index.json
data/knowledge_graph/index_historico.json
data/knowledge_graph/health_check_log.jsonl
data/knowledge_graph/**/.etl_process_cache.json
data/knowledge_graph/icts.json
data/knowledge_graph/.ict_theme_map_cache.json

# Caches LLM file-based na raiz (regeneráveis; não versionar)
.enrichment_cache.json
.match_embedding_cache.json

# OS
.DS_Store
*.DS_Store

# IDEs
.vscode/
.idea/
.claude/

# Node
frontend/node_modules/
frontend/.next/
frontend/.env.local

# Logs
*.log

# RAG eval — outputs gerados a cada run
eval_results/
eval_writing*.json
eval_matching_results.json

# macOS
.DS_Store

# Python virtualenv
.venv/

# Supabase CLI internal state
supabase/.branches/
supabase/.temp/
*.bak
data/knowledge_graph/.discovery_ledger.json
.eligibility_cache.json

# Hipergrado — artefatos derivados (durabilidade via kg_store/PG, não git)
data/knowledge_graph/hypergraphs/
data/knowledge_graph/graph/
data/hyper_extract_output*/
# Spike KG estrutura-consciente — export/obsidian (derivado, vive no kg_spike)
data/kg_spike_vault/
# Higiene de Conceitos (PR3): planos auditáveis + canon map (kg_store) + backups
data/knowledge_graph/canonicalization/
data/knowledge_graph/concept_canon.json
data/knowledge_graph/programa_canon.json
data/knowledge_graph.bak.*/

# Ferramentas locais de agentes (não são produto)
.codex/
graphify-out/
CLAUDE.md
.gitattributes
